[project]
name = "report-builder-tests"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pytest>=8.0",
    "pytest-playwright>=0.6.2",
    "playwright>=1.49",
]

[tool.pytest.ini_options]
base_url = "http://localhost:8010"
testpaths = ["."]
